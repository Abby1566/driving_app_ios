name: driving_app_ios
description: A new Flutter project.
publish_to: 'none'

# 關鍵修正：加入版本號（格式為：主版本.次版本.修訂版+組建編號）
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ... 其他套件保持不變 ...
